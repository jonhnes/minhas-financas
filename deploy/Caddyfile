{$APP_HOST} {
	encode zstd gzip

	@backend path /api/* /up /rails/*
	reverse_proxy @backend web:3000

	root * /srv/frontend

	header /assets/* Cache-Control "public, max-age=31536000, immutable"

	try_files {path} /index.html
	file_server
}
